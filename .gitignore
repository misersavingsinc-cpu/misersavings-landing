# Don't commit exported website zips
MiserSavings - website.zip
*.zip

# Node modules — installed via `npm install` for the blog build script
node_modules/
.DS_Store
.DS_Store
MISER_FACTS.md
BLOG_EDITOR.md
blog-drafts/

# Local scratch — never deploy
_to_delete/
# Stale duplicate of an older homepage; not on main, not linked, not in sitemap
download/
